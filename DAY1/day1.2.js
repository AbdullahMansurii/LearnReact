//todo REAL DOM vs VIRTUAL DOM
//todo REAL DOM is the actual DOM of the browser
//*difference between real dom and virtual dom is that real dom is the actual DOM of the browser, virtual dom is the copy of the actual DOM of the browser
//*virtual dom is used to render the DOM, real dom is used to manipulate the DOM
//*vitual dom compares the real dom and the virtual dom and if the real dom is different from the virtual dom, it will update the real dom

//todo JSX
//todo JSX is a syntax extension for javascript
//* JSX is used to write HTML in javascript

function Hero(){
    console.log('hello');
}
Hero(); //*OR <Hero />  //u can call a function in jsx in both ways 